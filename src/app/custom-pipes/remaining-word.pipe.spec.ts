import { RemainingWordPipe } from './remaining-word.pipe';

describe('RemainingWordPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingWordPipe();
    expect(pipe).toBeTruthy();
  });
});
