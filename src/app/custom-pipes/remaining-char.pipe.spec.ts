import { RemainingCharPipe } from './remaining-char.pipe';

describe('RemainingCharPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingCharPipe();
    expect(pipe).toBeTruthy();
  });
});
