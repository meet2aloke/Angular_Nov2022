import { TextinputDirective } from './textinput.directive';

describe('TextinputDirective', () => {
  it('should create an instance', () => {
    const directive = new TextinputDirective();
    expect(directive).toBeTruthy();
  });
});
