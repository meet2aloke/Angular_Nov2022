import { ValidemailDirective } from './validemail.directive';

describe('ValidemailDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidemailDirective();
    expect(directive).toBeTruthy();
  });
});
